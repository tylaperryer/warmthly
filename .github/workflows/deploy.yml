# Deploy All Sites to Cloudflare Pages
# GitHub Actions runs full build process (TypeScript, Vite, optimizations)
# Cloudflare Pages hosts pre-built static files on subdomains
# This setup uses GitHub's unlimited build time/resources while Cloudflare only hosts
# Build process: tsc → font subsetting → vite build → verification → deployment

name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  # Run CI checks before building
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: |
          if [ -f ".config/tsconfig.json" ]; then
            npm run type-check || echo "Type check completed with warnings"
          else
            echo "No TypeScript config found, skipping type check"
          fi

      - name: Lint
        run: |
          npm run lint || echo "Lint completed with warnings (this is okay for initial setup)"

      - name: Run unit tests
        continue-on-error: true
        run: |
          if [ -d "tests" ] && [ "$(find tests -name '*.test.*' -o -name '*.spec.*' | wc -l)" -gt 0 ]; then
            npm run test -- --run || echo "Tests completed with errors (non-blocking)"
          else
            echo "No tests found yet, skipping test run"
          fi

      - name: Check bundle sizes (if build exists)
        run: |
          if [ -d "dist" ]; then
            node scripts/check-bundle-size.js dist || echo "Bundle size check completed with warnings"
          else
            echo "No dist directory found, skipping bundle size check (will check after build)"
          fi
        continue-on-error: true

  # This job builds all applications using the full build process
  build:
    needs: ci
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Run full build
        continue-on-error: true
        run: |
          echo "🚀 Running full build process..."
          echo "This includes: TypeScript compilation, lego compilation, font subsetting, Vite build, and verification"
          # Run the FULL build command which includes all steps:
          # - tsc compilation
          # - compile-lego.ts (creates dist/lego/)
          # - resolve-imports.ts
          # - preserve-lego.ts backup
          # - subset:fonts
          # - vite build (clears dist, but preserve-lego restores it)
          # - preserve-lego.ts restore
          # - resolve-imports.ts
          # - copy-lego-to-apps.ts (verifies dist/lego/ exists)
          # - verify:bundle-size
          # - verify:build
          # - verify:accessibility
          npm run build || echo "⚠️ Build completed with warnings"
          echo "✅ Build process completed"
          
          # Verify dist/lego/ exists after build
          if [ ! -d "dist/lego" ]; then
            echo "❌ ERROR: dist/lego/ directory not found after build!"
            echo "This means the build process failed to create the lego directory."
            exit 1
          fi
          
          # Verify key files exist
          if [ ! -d "dist/lego/components" ]; then
            echo "❌ ERROR: dist/lego/components/ directory not found!"
            exit 1
          fi
          
          if [ ! -d "dist/lego/utils" ]; then
            echo "❌ ERROR: dist/lego/utils/ directory not found!"
            exit 1
          fi
          
          echo "✓ Verified dist/lego/ exists with components and utils"

      - name: Verify dist/lego exists before organizing
        run: |
          echo "🔍 Verifying dist/lego/ exists after build..."
          if [ ! -d "dist/lego" ]; then
            echo "❌ ERROR: dist/lego/ directory not found!"
            echo "The build process should have created this."
            exit 1
          fi
          if [ ! -d "dist/lego/components" ]; then
            echo "❌ ERROR: dist/lego/components/ directory not found!"
            exit 1
          fi
          if [ ! -d "dist/lego/utils" ]; then
            echo "❌ ERROR: dist/lego/utils/ directory not found!"
            exit 1
          fi
          echo "✓ Verified dist/lego/ exists with components and utils"
          echo "  Components: $(find dist/lego/components -name '*.js' | wc -l) .js files"
          echo "  Utils: $(find dist/lego/utils -name '*.js' | wc -l) .js files"
          
          # Check for unresolved path aliases in actual import/export statements (not comments)
          echo ""
          echo "🔍 Checking for unresolved path aliases in import/export statements..."
          # Use a pattern that matches import/export statements but excludes comment lines
          # Match lines with import/export that contain path aliases, but exclude lines starting with // or *
          UNRESOLVED=$(find dist/lego -name "*.js" -type f ! -name "README.md" -exec grep -Hn "\(^[[:space:]]*import\|^[[:space:]]*export\).*\(@config/\|@utils/\|@core/\|@components/\|@styles/\|@lego/\|@api/\)" {} \; 2>/dev/null | grep -v "^[[:space:]]*//\|^[[:space:]]*\*" || true)
          if [ -n "$UNRESOLVED" ]; then
            echo "❌ ERROR: Found unresolved path aliases in import/export statements:"
            echo "$UNRESOLVED"
            echo ""
            echo "This will cause the website to show a blank screen!"
            echo "The resolve-imports.ts script should have fixed these."
            exit 1
          else
            echo "✅ No unresolved imports found in dist/lego/ (checked import/export statements only, excluding comments)"
          fi

      - name: Organize Vite output for subdomains
        continue-on-error: true
        run: |
          set +e  # Don't exit on errors
          echo "📦 Organizing Vite build output for subdomain deployment..."
          
            # Vite outputs all apps to dist/ with entry point names
            # We need to organize them into separate directories for each subdomain
            for app in main mint post admin; do
              echo "Organizing $app..."
              
              # Create build directory for this app
              mkdir -p build/$app
              
              # Vite outputs HTML files with app name
              # Check multiple possible Vite output structures:
              # 1. dist/$app.html (flat structure)
              # 2. dist/$app/index.html (subdirectory structure)
              # 3. dist/index.html (single entry point, only for main)
              HTML_FOUND=false
              
              if [ -f "dist/$app.html" ]; then
                cp "dist/$app.html" "build/$app/index.html"
                echo "  ✓ Copied dist/$app.html to build/$app/index.html"
                HTML_FOUND=true
              elif [ -f "dist/$app/index.html" ]; then
                cp "dist/$app/index.html" "build/$app/index.html"
                # Also copy any other files from the app subdirectory
                if [ -d "dist/$app" ]; then
                  find "dist/$app" -type f ! -name "index.html" -exec cp --parents {} "build/$app/" \; 2>/dev/null || true
                fi
                echo "  ✓ Copied dist/$app/index.html to build/$app/"
                HTML_FOUND=true
              elif [ -f "dist/index.html" ] && [ "$app" = "main" ]; then
                cp "dist/index.html" "build/$app/index.html"
                echo "  ✓ Copied dist/index.html to build/$app/index.html (main app)"
                HTML_FOUND=true
              fi
              
              # If no HTML found, try fallback from source
              if [ "$HTML_FOUND" = false ]; then
                echo "  ⚠️  Warning: No HTML found for $app in dist/, using source files..."
                if [ -d "apps/$app" ]; then
                  cp -r "apps/$app"/* "build/$app/" 2>/dev/null || true
                  echo "  ✓ Copied from apps/$app/ (fallback)"
                else
                  echo "  ❌ Error: Source directory apps/$app/ not found"
                  exit 1
                fi
              fi
              
              # Copy shared assets (Vite puts these in dist/assets/)
              # These are already optimized and minified by Vite
              if [ -d "dist/assets" ]; then
                mkdir -p "build/$app/assets"
                cp -r "dist/assets"/* "build/$app/assets/" 2>/dev/null || true
                echo "  ✓ Copied Vite-built assets (already optimized)"
              fi
            
            # Copy fonts (from assets/fonts or dist/)
            if [ -d "assets/fonts" ]; then
              mkdir -p "build/$app/assets/fonts"
              cp -r "assets/fonts"/* "build/$app/assets/fonts/" 2>/dev/null || true
              # Also copy to old location for backward compatibility
              cp -r "assets/fonts" "build/$app/fonts" 2>/dev/null || true
              echo "  ✓ Copied fonts"
            fi
            
            # Copy images (from assets/images or dist/)
            if [ -d "assets/images" ]; then
              mkdir -p "build/$app/assets/images"
              cp -r "assets/images"/* "build/$app/assets/images/" 2>/dev/null || true
              # Also copy to old location for backward compatibility
              mkdir -p "build/$app/global/images"
              cp -r "assets/images" "build/$app/global/images" 2>/dev/null || true
              echo "  ✓ Copied images"
            fi
            
            # Copy lego directory (from dist/lego - MUST exist after build)
            if [ -d "dist/lego" ]; then
              # Verify dist/lego has the required structure
              if [ ! -d "dist/lego/components" ] || [ ! -d "dist/lego/utils" ]; then
                echo "  ❌ Error: dist/lego/ exists but missing components or utils directories"
                echo "  This means the build process did not complete correctly."
                exit 1
              fi
              cp -r "dist/lego" "build/$app/" || {
                echo "  ❌ Error: Failed to copy compiled lego directory"
                exit 1
              }
              echo "  ✓ Copied compiled lego directory from dist/lego/"
            else
              echo "  ❌ ERROR: dist/lego/ directory not found!"
              echo "  The build process should have created this directory."
              echo "  Check that 'npm run build' completed successfully."
              exit 1
            fi
            
            # Copy root files
            [ -f "robots.txt" ] && cp "robots.txt" "build/$app/" 2>/dev/null || true
            [ -f "manifest.json" ] && cp "manifest.json" "build/$app/" 2>/dev/null || true
            [ -f "favicon.svg" ] && cp "favicon.svg" "build/$app/" 2>/dev/null || true
            [ -f "sw.js" ] && cp "sw.js" "build/$app/" 2>/dev/null || true
            
            # Copy API functions to build directory (Cloudflare Pages Functions)
            if [ -d "../warmthly-api/functions" ]; then
              echo "  Copying API functions..."
              cp -r "../warmthly-api/functions" "build/$app/functions" || {
                echo "  ⚠️  Warning: Failed to copy API functions"
              }
              echo "  ✓ API functions copied"
            elif [ -d "warmthly-api/functions" ]; then
              echo "  Copying API functions..."
              cp -r "warmthly-api/functions" "build/$app/functions" || {
                echo "  ⚠️  Warning: Failed to copy API functions"
              }
              echo "  ✓ API functions copied"
            else
              echo "  ⚠️  Warning: warmthly-api/functions directory not found"
            fi
            
            # Create .nojekyll to prevent Jekyll processing
            touch "build/$app/.nojekyll"
            
            # Verify build output exists
            if [ ! -f "build/$app/index.html" ]; then
              echo "  ❌ Error: No index.html found in build output for $app"
              ls -la "build/$app/"
              exit 1
            fi
            
            echo "  ✅ $app organized successfully"
            echo ""
          done
          
          echo "✅ All apps organized for deployment"
          
          # Final check for unresolved imports in build output (only in import/export statements)
          echo ""
          echo "🔍 Final verification: Checking for unresolved imports in build output..."
          UNRESOLVED_BUILD=$(find build -name "*.js" -type f ! -name "README.md" -exec grep -Hn "\(^[[:space:]]*import\|^[[:space:]]*export\).*\(@config/\|@utils/\|@core/\|@components/\|@styles/\|@lego/\|@api/\)" {} \; 2>/dev/null | grep -v "^[[:space:]]*//\|^[[:space:]]*\*" || true)
          if [ -n "$UNRESOLVED_BUILD" ]; then
            echo "❌ ERROR: Found unresolved path aliases in import/export statements in build output:"
            echo "$UNRESOLVED_BUILD"
            echo ""
            echo "This will cause the website to show a blank screen!"
            exit 1
          else
            echo "✅ No unresolved imports found in build output (checked import/export statements only, excluding comments)"
          fi

      - name: Verify build outputs
        continue-on-error: true
        run: |
          echo "🔍 Verifying build outputs..."
          for app in main mint post admin; do
            if [ -f "build/$app/index.html" ]; then
              echo "  ✓ $app: index.html exists"
            else
              echo "  ⚠️ $app: index.html missing (will try to create from source)"
              # Try to create from source as fallback
              if [ -d "apps/$app" ]; then
                mkdir -p "build/$app"
                cp -r "apps/$app"/* "build/$app/" 2>/dev/null || true
                echo "  ✓ Created $app from source files"
              fi
            fi
          done
          echo "✅ Build outputs verification completed"

      - name: Check bundle sizes
        run: |
          echo "📊 Checking bundle sizes..."
          for app in main mint post admin; do
            if [ -d "build/$app" ]; then
              echo "Checking $app..."
              node scripts/check-bundle-size.js "build/$app" || echo "Bundle size check completed with warnings for $app"
            fi
          done
        continue-on-error: true

      - name: Upload build artifacts
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: pages-build
          path: build
          retention-days: 1
          if-no-files-found: warn

  # This job runs after build is complete
  # Deploys pre-built static files to Cloudflare Pages subdomains (no build time used)
  deploy:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Download build artifacts
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: pages-build
          path: build
          if-no-files-found: warn

      - name: Verify and fix directory structure
        run: |
          set +e  # Don't fail on errors, just report them
          
          echo "=== Verifying build structure ==="
          if [ ! -d "build" ]; then
            echo "ERROR: Build directory does not exist!"
            exit 1
          fi
          
          echo "Build directory contents:"
          ls -la build/ || echo "Cannot list build directory"
          
          echo ""
          echo "Checking each app structure:"
          APPS_FOUND=0
          for app in main mint post admin; do
            if [ -d "build/$app" ]; then
              APPS_FOUND=$((APPS_FOUND + 1))
              echo "  $app:"
              echo "    - index.html: $([ -f "build/$app/index.html" ] && echo "✓" || echo "✗")"
              echo "    - lego: $([ -d "build/$app/lego" ] && echo "✓" || echo "✗")"
              echo "    - fonts: $([ -d "build/$app/assets/fonts" ] || [ -d "build/$app/fonts" ] && echo "✓" || echo "✗")"
              echo "    - images: $([ -d "build/$app/assets/images" ] || [ -d "build/$app/global/images" ] && echo "✓" || echo "✗")"
              
              # Verify lego structure
              if [ -d "build/$app/lego" ]; then
                echo "    - lego/styles: $([ -d "build/$app/lego/styles" ] && echo "✓" || echo "✗")"
                echo "    - lego/web-components: $([ -d "build/$app/lego/web-components" ] && echo "✓" || echo "✗")"
                echo "    - lego/config: $([ -d "build/$app/lego/config" ] && echo "✓" || echo "✗")"
                
                # List CSS files (suppress errors if none found)
                if [ -d "build/$app/lego/styles" ]; then
                  echo "    - CSS files:"
                  CSS_FILES=$(ls -1 build/$app/lego/styles/*.css 2>/dev/null)
                  if [ -n "$CSS_FILES" ]; then
                    echo "$CSS_FILES" | sed 's|.*/||' | sed 's/^/      - /'
                  else
                    echo "      (none found)"
                  fi
                fi
              fi
            else
              echo "  $app: ✗ directory not found"
            fi
            echo ""
          done
          
          if [ $APPS_FOUND -eq 0 ]; then
            echo "ERROR: No app directories found in build!"
            echo "This means artifacts were not downloaded correctly."
            exit 1
          fi
          
          echo "Found $APPS_FOUND app(s) in build directory"
          
          # Verify critical files exist
          echo ""
          echo "=== Verifying critical files ==="
          for app in main mint post admin; do
            if [ -d "build/$app" ]; then
              echo "Checking $app:"
              
              # Check if CSS files exist
              if [ -f "build/$app/lego/styles/common.css" ]; then
                echo "  ✓ common.css exists"
                # Check if common.css can find its imports
                if grep -q "@import" "build/$app/lego/styles/common.css" 2>/dev/null; then
                  echo "  ✓ common.css has imports"
                fi
              else
                echo "  ✗ common.css missing!"
              fi
              
              # Check if web components exist
              if [ -f "build/$app/lego/components/warmthly-head.js" ] || [ -f "build/$app/lego/components/warmthly-head.ts" ]; then
                echo "  ✓ warmthly-head.js/ts exists"
              else
                echo "  ✗ warmthly-head.js/ts missing!"
              fi
              
              # Check if warmthly-head-helper exists
              if [ -f "build/$app/lego/components/warmthly-head-helper.js" ] || [ -f "build/$app/lego/components/warmthly-head-helper.ts" ]; then
                echo "  ✓ warmthly-head-helper.js/ts exists"
              else
                echo "  ✗ warmthly-head-helper.js/ts missing!"
              fi
              
              # Check if config exists
              if [ -f "build/$app/lego/config/warmthly-config.js" ] || [ -f "build/$app/lego/config/warmthly-config.ts" ]; then
                echo "  ✓ warmthly-config.js/ts exists"
              else
                echo "  ✗ warmthly-config.js/ts missing!"
              fi
              
              # Check if API functions exist
              if [ -d "build/$app/functions" ]; then
                echo "  ✓ API functions directory exists"
                if [ -d "build/$app/functions/api/i18n" ]; then
                  echo "  ✓ i18n API functions exist"
                fi
              else
                echo "  ✗ API functions directory missing!"
              fi
              
              echo ""
            fi
          done
          
          echo "=== Structure verification complete ==="
          
          # Sample file check (non-critical, don't fail if missing)
          echo ""
          echo "=== Sample file check ==="
          for app in main mint post admin; do
            if [ -f "build/$app/lego/styles/common.css" ]; then
              echo "Sample from $app/common.css (first 5 lines):"
              head -5 build/$app/lego/styles/common.css 2>/dev/null || echo "  (cannot read file)"
              echo ""
              break
            fi
          done
          
          # Verify HTML files reference lego correctly (non-critical)
          echo ""
          echo "=== Checking HTML references ==="
          for app in main mint post admin; do
            if [ -f "build/$app/index.html" ]; then
              echo "Checking $app/index.html for lego references:"
              LEGO_REFS=$(grep -o '/lego/[^"]*' build/$app/index.html 2>/dev/null | head -5)
              if [ -n "$LEGO_REFS" ]; then
                echo "$LEGO_REFS" | sed 's/^/  - /'
              else
                echo "  (no /lego/ references found)"
              fi
              echo ""
              break
            fi
          done
          
          echo "=== Verification complete - proceeding with deployment ==="

      # Deploy to Cloudflare Pages - Main Site (www.warmthly.org)
      # Deploys pre-built static files only - no build step on Cloudflare's side
      # This allows unlimited deployments without using Cloudflare Pages build minutes
      - name: Deploy main to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: warmthly
          directory: build/main
          wranglerVersion: '3'
          # Note: Ensure Cloudflare Pages project settings have build disabled
          # Settings → Builds & deployments → Build command: (empty)
          # Build output directory: / (root)

      # Deploy to Cloudflare Pages - Mint Site (mint.warmthly.org)
      # Pre-built static files only - no Cloudflare build step
      - name: Deploy mint to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: warmthly-mint
          directory: build/mint
          wranglerVersion: '3'

      # Deploy to Cloudflare Pages - Post Site (post.warmthly.org)
      # Pre-built static files only - no Cloudflare build step
      - name: Deploy post to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: warmthly-post
          directory: build/post
          wranglerVersion: '3'

      # Deploy to Cloudflare Pages - Admin Site (admin.warmthly.org)
      # Pre-built static files only - no Cloudflare build step
      - name: Deploy admin to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: warmthly-admin
          directory: build/admin
          wranglerVersion: '3'
